version: v2

modules:
  - path: proto
    name: buf.build/bannote/reservation-consumer

deps:
  - buf.build/bannote/studyroom-service

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH

breaking:
  use:
    - FILE
